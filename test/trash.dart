// Column(
// children: _.areaCont
// .map((item) => GestureDetector(
//     onTap: () {},
//     child: CardCont(
//       item: item,
//     )))
// .toList(),
// )


// _.area
// .map(
// (item) => InkWell(
// onTap: () {
// Get.toNamed(Routes.contenidoArea, arguments: item);
// },
// child: CardFiles(color: Color(int.parse('0xFF' + item.color.toString())), text: item.nombre.toString())),
// )
// .toList(),